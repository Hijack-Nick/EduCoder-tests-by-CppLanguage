#include<iostream>
#include<string>
using namespace std;
int main()
{
	string str;
	getline(cin,str);
	string str1=str;
	int N=str.size();
	for(int i=0;i<str.size();i++)
	{
		str1[i]=str[N-1-i];
	}
	cout << str1;
	return 0;
}
